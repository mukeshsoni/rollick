import React from 'react'
import ReactDOM from 'react-dom'

import App from 'rollick/app.js'

let container = document.getElementById('container')

let component = ReactDOM.render(React.createElement(App), container)
